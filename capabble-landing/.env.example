# Client app origin (used for login URL)
NEXT_PUBLIC_APP_ORIGIN=https://app.capabble.cloud

# API server origin (used for signup API calls)
NEXT_PUBLIC_API_ORIGIN=https://api.capabble.cloud

# Root app domain (used to build tenant subdomain redirect URLs)
NEXT_PUBLIC_ROOT_APP_DOMAIN=capabble.cloud
